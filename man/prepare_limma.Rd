% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prepare_data.R
\name{prepare_limma}
\alias{prepare_limma}
\title{Prepare limma results for MetaVolcanoR}
\usage{
prepare_limma(
  limma_result,
  gene_column = "GeneID",
  fc_column = "logFC",
  pval_column = "P.Value",
  se_column = NULL,
  ci_left = "CI.L",
  ci_right = "CI.R"
)
}
\arguments{
\item{limma_result}{A data.frame from limma::topTable()}

\item{gene_column}{Column name containing gene identifiers (default: "GeneID")}

\item{fc_column}{Column name containing log fold changes (default: "logFC")}

\item{pval_column}{Column name containing p-values (default: "P.Value")}

\item{se_column}{Column name containing standard errors. If NULL, will try
to calculate from CI columns or t-statistic}

\item{ci_left}{Column name for left CI limit (default: "CI.L")}

\item{ci_right}{Column name for right CI limit (default: "CI.R")}
}
\value{
A data.frame with columns: Symbol, Log2FC, pvalue, CI.L, CI.R
}
\description{
Converts limma topTable results to MetaVolcanoR format
}
\examples{
\dontrun{
library(limma)
fit <- lmFit(eset, design)
fit <- eBayes(fit)
top <- topTable(fit, number = Inf)

# Prepare for MetaVolcanoR
deg_table <- prepare_limma(top)
}
}
