% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prepare_data.R
\name{prepare_edger}
\alias{prepare_edger}
\title{Prepare edgeR results for MetaVolcanoR}
\usage{
prepare_edger(edger_result, gene_column = NULL)
}
\arguments{
\item{edger_result}{A data.frame from edgeR::topTags()}

\item{gene_column}{Column name containing gene identifiers (default: uses rownames)}
}
\value{
A data.frame with columns: Symbol, Log2FC, pvalue, CI.L, CI.R
}
\description{
Converts edgeR topTags results to MetaVolcanoR format
}
\examples{
\dontrun{
library(edgeR)
y <- DGEList(counts = counts, group = group)
y <- calcNormFactors(y)
design <- model.matrix(~group)
y <- estimateDisp(y, design)
fit <- glmFit(y, design)
lrt <- glmLRT(fit)
top <- topTags(lrt, n = Inf)

# Prepare for MetaVolcanoR  
deg_table <- prepare_edger(top$table)
}
}
